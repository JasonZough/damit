import React, { PureComponent } from 'react';
import styled from 'styled-components';

class MinimapPanel extends PureComponent {
  public render() {
    return <MinimapPanelContainer />;
  }
}

const MinimapPanelContainer = styled.div``;

export default MinimapPanel;
